# Documentation Rules

- Feature implemented → documentation updated.
- Requirement changes MUST be reflected in `01-requirements`.
- Architecture-impacting changes MUST be reflected in `02-architecture` and `06-decisions`.
- Status changes MUST be recorded under `07-status`.
