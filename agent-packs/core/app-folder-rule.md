# /app Folder Rule

- All implementation code MUST live under `/app`.
- Non-code artifacts belong in documentation/governance locations.
- Agents MUST treat code outside `/app` as a policy violation.
